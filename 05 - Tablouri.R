# Construirea unui tablou tridimensional
tablou <- array(dim = c(3, 3, 3)); tablou

# Popularea cu valori a unui tablou
tablou[,,1] <- c(1, 2, 3, 4, 5, 6, 7, 8, 9); tablou

# Indecsii unui tablou
tablou[1,2,3] # tablou[rand, coloana, obiect]
