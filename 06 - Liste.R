# Crearea unei liste
lista <- list(
  "Un vector" = varsta, 
  "Un factor" = grade, 
  "O constanta" = 54,
  "O matrice" = matrice,
  "Un tablou" = tablou)
lista

# Apelarea elementelor din lista
lista$`Un tablou`
lista[[5]]
